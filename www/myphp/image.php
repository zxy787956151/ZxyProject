<?php 
	// $im = imagecreate(100,100);

	// $red = imagecolorallocate($im,225, 0, 0);
	// $green = imagecolorallocate($im,0, 225, 0);

	// // if (function_exists("imagegif")) {
	// // 	header("Content-type:image/gif");
	// // 	imagegif($im);
	// // }elseif (function_exists("imagejpeg")) {
	// // 	header("Content-type:image/jpeg");
	// // 	imagegif($im,"",0.5);	
	// //}//and so on

	// imagefill($im, 10, 10,$green);			

	// header("Content-type:image/png");
	// imagepng($im);
	// imagedestroy($im);


	// $img = imagecreate(150,150);
	// $bg = imagecolorallocate($img, 225, 225, 225);	//白色

	// $black=imagecolorallocate($img, 0, 0, 0);
	// $string = "LAMPBorder";

	// imagestring($img, 3, 28, 70, $string, $black);
	// imagestringup($img, 3, 59, 115, $string, $black);

	// for ($i=0,$j=strlen($string); $i <strlen($string) ; $i++,$j--) { 
	// 	imagechar($img, 3, 10*($i+1), 10*($i+2), $string[$i], $black);
	// 	imagecharup($img, 3, 10*($i+1), 10*($j+2), $string[$i], $black);
	// }

	// header("Content-type:image/png");
	// imagepng($img);
	// imagedestroy($img);


	// $ima = imagecreatetruecolor(400, 30);

	// $white = imagecolorallocate($ima, 225, 225, 225);
	// $grey = imagecolorallocate($ima, 128, 128, 128);		//灰色
	// $black = imagecolorallocate($ima, 0, 0, 0);

	// imagefilledrectangle($ima, 0, 0, 399, 29, $white);

	// $text = iconv("UTF-8","UTF-8","LAMP 兄弟连——无兄弟，不编程!"); 

	// $font = 'simsunb.ttf';

	// imagettftext($ima, 20, 0, 12, 21, $grey, $font, $text);
	// imagettftext($ima, 20, 0, 10, 20, $black, $font, $text);

	// header("Content-type:image/png");
	// imagepng($ima);
	// imagedestroy($ima);	

	  


 ?>